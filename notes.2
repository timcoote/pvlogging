solution design:
- build rpm for aurora
- build rpm for plotting, that depends on the shiny-server rpm, and then drops in the shiny apps and new /etc/shiny-server/shiny-server.conf

--  or should these use puppet / is this all bound together with Rake/puppet?
